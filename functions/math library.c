#include<stdio.h>
#include<math.h>
int main()
{
    float a=0.5;
    float w,x,y,z;
    w=sin(a);
    x=cos(a);
    y=tan(a);
    z=pow(a,z);
    printf("%d\n%d\n%d\n%d",w,x,y,z);
    return 0;
}
